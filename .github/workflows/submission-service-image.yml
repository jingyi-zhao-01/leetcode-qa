---
name: submission-service-image

on:
    push:
        branches:
            - main
        tags:
            - v*
        paths:
            - services/shared/prisma/**
            - services/leetcode-submission-service/**
            - package.json
            - package-lock.json
            - docker/leetcode-submission-service.Dockerfile
            - .github/workflows/submission-service-image.yml
    pull_request:
        paths:
            - services/shared/prisma/**
            - services/leetcode-submission-service/**
            - package.json
            - package-lock.json
            - docker/leetcode-submission-service.Dockerfile
            - .github/workflows/submission-service-image.yml
    workflow_dispatch:

permissions:
    contents: read
    packages: write

env:
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
    lint:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Lint services
              run: npm run lint:services

    build:
        needs: lint
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Build submission service
              run: npm run --workspace services/leetcode-submission-service build

    test:
        needs: build
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Run submission service tests
              run: npm run --workspace services/leetcode-submission-service test

    smoke:
        needs: test
        runs-on: ubuntu-latest
        services:
            postgres:
                image: postgres:16
                env:
                    POSTGRES_USER: postgres
                    POSTGRES_PASSWORD: postgres
                    POSTGRES_DB: postgres
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd "pg_isready -U postgres -d postgres"
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4

            - name: Build local smoke image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: docker/leetcode-submission-service.Dockerfile
                  load: true
                  tags: leetcode-submission-service:smoke

            - name: Start submission smoke container
              run: |
                  docker run -d --rm \
                    --name submission-service-smoke \
                    --network host \
                    -e DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/postgres \
                    leetcode-submission-service:smoke

            - name: Wait for submission port
              run: |
                  for attempt in {1..30}; do
                    if timeout 1 bash -c '</dev/tcp/127.0.0.1/3000' 2>/dev/null; then
                      exit 0
                    fi

                    if ! docker ps --format '{{.Names}}' | grep -qx submission-service-smoke; then
                      docker logs submission-service-smoke || true
                      exit 1
                    fi

                    sleep 1
                  done

                  docker logs submission-service-smoke || true
                  exit 1

            - name: Stop submission smoke container
              if: always()
              run: docker rm -f submission-service-smoke || true

    build-and-push:
        needs: smoke
        runs-on: ubuntu-latest
        if: github.event_name != 'pull_request'

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v4

            - name: Log in to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Extract metadata
              id: meta
              uses: docker/metadata-action@v6
              with:
                  images: ghcr.io/${{ github.repository_owner }}/leetcode-submission-service
                  tags: |
                      type=semver,pattern={{version}}
                      type=semver,pattern={{major}}.{{minor}}
                      type=semver,pattern={{major}}
                      type=sha
                      type=ref,event=branch
                      type=raw,value=latest,enable={{is_default_branch}}

            - name: Build and push image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: docker/leetcode-submission-service.Dockerfile
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
